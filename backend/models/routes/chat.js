const express = require('express');
const router = express.Router();

// Placeholder for chat routes
router.get('/', (req, res) => {
  res.send('Chat functionality coming soon!');
});

module.exports = router;
